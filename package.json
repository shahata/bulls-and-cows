{
  "name": "bulls-and-cows",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://shahata.github.io/bulls-and-cows",
  "dependencies": {
    "lodash": "^4.17.10",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
